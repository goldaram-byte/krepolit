// Shared constants for the sample-order flow (cart, API route, /samples page).
export const MAX_SAMPLE_ITEMS = 5;
// Flat fee, credited against the final floor-covering order (SPEC.md §5/§7).
// No per-product/per-distance pricing in the MVP.
export const SAMPLE_DELIVERY_PRICE = 300;
