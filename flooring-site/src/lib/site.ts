export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "http://localhost:3000";

// TODO: replace with the real company name/details once decided (see also
// Header.tsx). Used for the Organization JSON-LD (SPEC.md §6).
export const SITE_NAME = "Напольные покрытия";
