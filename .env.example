# Скопируйте в .env и заполните
# Домен для авто-HTTPS. Если домена нет — оставьте :80 (доступ по IP, http).
DOMAIN=crm.example.ru
# Секрет для подписи сессий (сгенерируйте: openssl rand -hex 32)
JWT_SECRET=ЗАМЕНИТЕ_НА_СЛУЧАЙНУЮ_СТРОКУ
